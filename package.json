{
  "name": "google-reader-api-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Contract test suite for the Google Reader-compatible API (FreshRSS reference). Exercises all endpoints so a new greader server implementation can be validated for compatibility with established client apps.",
  "type": "commonjs",
  "scripts": {
    "test": "node --test test/*.test.js",
    "test:verbose": "node --test --test-reporter=spec test/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
